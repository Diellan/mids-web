// Converted from C# ExtendedBitmap.cs
// Note: This class uses System.Drawing.Bitmap which is Windows Forms specific
// In TypeScript/web context, this would need to be adapted for image handling
// (e.g., using HTMLImageElement, Canvas, or a web image library)

export interface Size {
  Width: number;
  Height: number;
}

export class ExtendedBitmap {
  // Note: Bitmap and Graphics are Windows Forms specific
  // In TypeScript, this would use web-compatible image types
  // For now, creating a placeholder structure

  constructor(imageSize?: Size, width?: number, height?: number, file?: string) {
    // Note: Implementation would initialize image based on parameters
  }

  // Note: Dispose, Clone, and other methods would need web-compatible implementations
}

