// Converted from C# Print.cs
// Note: This class uses System.Drawing.Printing which is Windows Forms specific
// In TypeScript/web context, printing would use browser print APIs or PDF generation libraries

enum PageType {
  ProfileShort,
  ProfileLong,
  History,
  Stats
}

export class Print {
  // Note: PrintDocument is Windows Forms specific
  // In TypeScript, this would use web-compatible printing (window.print(), PDF libraries, etc.)
  
  // Note: Implementation would handle printing functionality
}

